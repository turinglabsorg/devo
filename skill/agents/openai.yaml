interface:
  display_name: "Devo"
  short_description: "Cloud ops checks for GCP and AWS"
  default_prompt: "Use $devo to audit cloud health, logs, costs, and service configuration across GCP and AWS."
