{
  "name": "@turinglabs/devo-skill",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "bin": {
    "devo": "./index.js"
  },
  "scripts": {
    "check": "node --check index.js && for f in scripts/*.mjs; do node --check \"$f\" || exit 1; done && sh -n hooks/gcloud-guard.sh",
    "doctor": "node index.js doctor --provider all",
    "test": "node --test test/*.test.mjs"
  }
}
